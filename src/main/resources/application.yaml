spring:
  application:
    name: toeic_vova

  datasource:
    url: jdbc:mysql://localhost:3306/toeic_app
    username: root
    password: voca_p123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true